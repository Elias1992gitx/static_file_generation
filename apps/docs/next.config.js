// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;


const nextConfig = {
    output: 'export',
    distDir: 'dist',
    images: {
      unoptimized: true
    }
  };
  
  export default nextConfig;